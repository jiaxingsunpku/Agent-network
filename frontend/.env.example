# 前端环境变量示例（复制为 .env / .env.local 后按需修改；.env* 已被忽略，勿入库真实值）。
#
# 二选一接入网关：
#
# 1) 开发期同源反代（默认，推荐）：不设 VITE_AGENT_NETWORK_API_BASE，
#    由 vite dev/preview 把 /api/* 反代到下面的网关地址。
VITE_GATEWAY_PROXY=http://127.0.0.1:8000
#
# 2) 客户端直连绝对地址（生产/一体化部署，或前端与网关同机不同口直连）：
#    设了它就不走 dev proxy，浏览器直接请求该 base + /api/agent-network/*。
# VITE_AGENT_NETWORK_API_BASE=http://127.0.0.1:8000
